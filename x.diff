diff --git a/local/library/iosxr_rollback b/local/library/iosxr_rollback
index c0973cc..0dc1d9f 100644
--- a/local/library/iosxr_rollback
+++ b/local/library/iosxr_rollback
@@ -128,18 +128,18 @@ def main():
     force = args['force']
   
     reload_command = 'rollback configuration '
-    if rollback_id != None:
-        reload_command = reload_command + rollback_id
-    if to_id != None:
-        reload_command = reload_command + 'to %s ' % to_id
-    if to_excl_id != None:
-        reload_command = reload_command + 'to-exclude %s ' % to_excl_id
-    if last_n != None:
-        reload_command = reload_command + 'last %s ' % last_n
+    if rollback_id is not None:
+        reload_command += rollback_id
+    if to_id is not None:
+        reload_command += 'to %s ' % to_id
+    if to_excl_id is not None:
+        reload_command += 'to-exclude %s ' % to_excl_id
+    if last_n is not None:
+        reload_command += 'last %s ' % last_n
     if force is True:
-        reload_command = reload_command + 'force '
-    if label != None:
-        reload_command = reload_command + 'label %s ' % label
+        reload_command += 'force '
+    if label is not None:
+        reload_command += 'label %s ' % label
     response = execute_command(module, reload_command)
   
     result = dict(changed=True)
diff --git a/local/library/iosxr_show_install_version b/local/library/iosxr_show_install_version
index 8d242e5..f370ef6 100644
--- a/local/library/iosxr_show_install_version
+++ b/local/library/iosxr_show_install_version
@@ -73,6 +73,5 @@ def main():
     result['stdout'] = info.version.log
     return module.exit_json(**result)
 
-
 if __name__ == "__main__":
     main()
diff --git a/local/samples/show_diff_config b/local/samples/show_diff_config
index aa339a7..0b11898 100755
--- a/local/samples/show_diff_config
+++ b/local/samples/show_diff_config
@@ -1,6 +1,6 @@
 #!/bin/bash
 if [ $# -lt 1 ]; then
-    echo "usage: show_diff <config_file>"
+    echo "usage: show_diff_config <config_file>"
     exit 1
 fi
 ansible-playbook iosxr_diff_config.yml -e "config_file=$1"
diff --git a/remote/library/iosxr_install_config b/remote/library/iosxr_install_config
index 6151a14..6a6cc9e 100644
--- a/remote/library/iosxr_install_config
+++ b/remote/library/iosxr_install_config
@@ -71,7 +71,7 @@ def main():
     label = args['label']
   
     load_command = 'config -f %s ' % cfg_name
-    if label != None:
+    if label is not None:
         load_command = load_command + '-l "%s"' % label
     command = "source /etc/profile ; nsenter -t 1 -n -- %s" % load_command
     (rc, out, err) = module.run_command(command, use_unsafe_shell=True)
diff --git a/remote/library/iosxr_install_package b/remote/library/iosxr_install_package
index 931afb2..aff7b68 100644
--- a/remote/library/iosxr_install_package
+++ b/remote/library/iosxr_install_package
@@ -132,7 +132,7 @@ def install_add(module, pkg_path, pkg_name):
         response = [pkg_name + " package is already active\n"]
     elif is_package_already_added(module, pkg_name):
         response = [pkg_name + " package is already added\n"]
-    elif pkg_path == None:
+    elif pkg_path is None:
         module.fail_json(msg="package path required")
     else:
         command = ("install add source " +
diff --git a/remote/library/iosxr_reload b/remote/library/iosxr_reload
index c96de6f..9af3850 100644
--- a/remote/library/iosxr_reload
+++ b/remote/library/iosxr_reload
@@ -35,7 +35,7 @@ options:
     value: "yes" or other string
   force:
     description:
-      - force reaload without doing any cleanup
+      - force reload without doing any cleanup
     required: false
     default: false
 """
diff --git a/remote/library/iosxr_rollback b/remote/library/iosxr_rollback
index 2559251..52e6413 100644
--- a/remote/library/iosxr_rollback
+++ b/remote/library/iosxr_rollback
@@ -30,7 +30,7 @@ description:
 options:
   rollback_id:
     description:
-      - rollback configuration committted to particular id
+      - rollback configuration committed to particular id
         e.g. 1000000012
     required: false
     default: None
@@ -48,7 +48,7 @@ options:
     default: None
   last_n_committed:
     description:
-      - rollback configuration last N committted made
+      - rollback configuration last N committed made
         e.g. 5
     required: false
     default: None
@@ -109,18 +109,18 @@ def main():
     force = args['force']
 
     reload_command = 'source /etc/profile ; nsenter -t 1 -n -- config_rollback '
-    if rollback_id != None:
-        reload_command = reload_command + '-j %s ' % rollback_id
-    if to_id != None:
-        reload_command = reload_command + '-i %s ' % to_id
-    if to_excl_id != None:
-        reload_command = reload_command + '-u %s ' % to_excl_id
-    if last_n != None:
-        reload_command = reload_command + '-n %s ' % hex(last_n)
+    if rollback_id is not None:
+        reload_command += '-j %s ' % rollback_id
+    if to_id is not None:
+        reload_command += '-i %s ' % to_id
+    if to_excl_id is not None:
+        reload_command += '-u %s ' % to_excl_id
+    if last_n is not None:
+        reload_command += '-n %s ' % hex(last_n)
     if force is True:
-        reload_command = reload_command + '-f '
-    if label != None:
-        reload_command = reload_command + '-l %s ' % label
+        reload_command += '-f '
+    if label is not None:
+        reload_command += '-l %s ' % label
     (rc, out, err) = module.run_command(reload_command, use_unsafe_shell=True)
   
     result = dict(changed=False)
